name: Spellcheck

on: [push]

jobs:
  spellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run spellcheck
        uses: rojopolis/spellcheck-github-actions@v0
